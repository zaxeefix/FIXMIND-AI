"use client";
import Link from "next/link";
import { useActionState, useEffect, useRef } from "react";
import { Logo, Stepper } from "@/components/ui";
import { CheckCircle2, Wrench } from "lucide-react";
import { createBooking } from "./actions";

export default function Book(){const[state,action,pending]=useActionState(createBooking,{error:""});const formRef=useRef<HTMLFormElement>(null);useEffect(()=>{try{const draft=JSON.parse(sessionStorage.getItem("fixmind-diagnosis-draft")||"{}") as Record<string,string>;const mapping:Record<string,string|undefined>={deviceCategory:draft.deviceCategory,brand:draft.brand,deviceModel:draft.deviceModel,service:draft.problemCategory?draft.problemCategory+" repair":undefined,notes:draft.description};Object.entries(mapping).forEach(([name,value])=>{const field=formRef.current?.elements.namedItem(name);if(value&&(field instanceof HTMLInputElement||field instanceof HTMLTextAreaElement||field instanceof HTMLSelectElement))field.value=value});void fetch("/api/session").then(r=>r.json()).then((session:{authenticated?:boolean;role?:string})=>{if(session.authenticated&&session.role==="customer"){const email=formRef.current?.elements.namedItem("customerEmail");if(email instanceof HTMLInputElement){const stored=document.cookie;void stored}}})}catch{}},[]);return <main className="min-h-screen bg-white"><header className="border-b"><div className="container flex h-[72px] items-center justify-between"><Logo/><Link href="/" className="text-sm font-semibold text-slate-600">Save & exit</Link></div></header><div className="border-b bg-slate-50 py-7"><div className="container"><Stepper current={4}/></div></div><section className="container py-12"><form ref={formRef} action={action} className="mx-auto max-w-3xl"><p className="eyebrow">Diagnosis to repair</p><h1 className="h1 mt-2 font-bold">Review and book your repair</h1><p className="muted">Your latest diagnosis details are carried forward automatically. Confirm the service, location, and schedule below.</p><div className="card mt-8 grid gap-5 sm:grid-cols-2"><Field label="Full name" name="customerName"/><Field label="Email" name="customerEmail" type="email"/><Field label="Phone" name="phone" type="tel"/><label><span className="label">Device category</span><select className="input" name="deviceCategory" required><option>Smartphone</option><option>Tablet</option><option>Laptop</option><option>Smartwatch</option></select></label><Field label="Brand" name="brand" placeholder="Apple"/><Field label="Device model" name="deviceModel" placeholder="iPhone 15 Pro"/><Field label="Service" name="service" placeholder="Battery replacement"/><Field label="Repair location" name="location" placeholder="FixMind Downtown"/><label className="sm:col-span-2"><span className="label">Appointment</span><input className="input" name="appointmentAt" type="datetime-local" required/></label><label className="sm:col-span-2"><span className="label">Diagnosis summary or notes <span className="font-normal text-slate-400">(optional)</span></span><textarea className="input min-h-28" name="notes" maxLength={1000}/></label>{state.error&&<p role="alert" className="sm:col-span-2 m-0 rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}</div><div className="card mt-5 flex flex-col gap-4 bg-blue-50/50 sm:flex-row sm:items-center"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-white text-blue-600"><Wrench/></span><div className="flex-1"><b>Repair summary ready</b><p className="muted m-0 text-sm">Final parts and price are confirmed after technician inspection.</p></div><b className="text-xl">$99 estimate</b></div><button disabled={pending} className="btn btn-primary mt-6 w-full sm:w-auto">{pending?"Confirming…":"Confirm booking"} <CheckCircle2 size={17}/></button></form></section></main>}
function Field({label,name,type="text",placeholder}:{label:string;name:string;type?:string;placeholder?:string}){return <label><span className="label">{label}</span><input className="input" name={name} type={type} placeholder={placeholder} required/></label>}
