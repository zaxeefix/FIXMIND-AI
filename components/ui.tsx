"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import Link from "next/link";import Image from "next/image";import {type ButtonHTMLAttributes,type ReactNode,useEffect,useState} from "react";import {ArrowRight,Check,Cpu,LogOut,Menu,Search,Sparkles,X} from "lucide-react";import {logout} from "@/app/logout/actions";
export function Logo({inverse=false}:{inverse?:boolean}){if(inverse)return <Link href="/" className="flex items-center gap-2 text-white no-underline"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><Cpu size={19}/></span><span className="text-lg font-bold tracking-tight">FixMind <span className="text-blue-400">AI</span></span></Link>;return <Link href="/" aria-label="FixMind AI home" className="block h-14 w-36 shrink-0 overflow-hidden"><Image src="/fixmind-logo.png" alt="FixMind AI — Diagnose Smarter. Repair Faster." width={144} height={56} priority className="h-full w-full object-contain"/></Link>}
export function Button({children,className="",...props}:ButtonHTMLAttributes<HTMLButtonElement>){return <button className={`btn btn-primary ${className}`} {...props}>{children}</button>}
export function TopNav(){const[open,setOpen]=useState(false);const[authenticated,setAuthenticated]=useState(false);useEffect(()=>{void fetch("/api/session",{cache:"no-store"}).then(response=>response.json()).then((data:{authenticated?:boolean})=>setAuthenticated(Boolean(data.authenticated))).catch(()=>setAuthenticated(false))},[]);return <header className="fixed inset-x-0 top-0 z-50 border-b border-slate-200/80 bg-white/90 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/90"><div className="container flex h-[72px] items-center justify-between gap-5"><Logo/><nav className="hidden items-center gap-6 text-sm font-medium text-slate-600 lg:flex dark:text-slate-300"><a href="/#features">Features</a><a href="/#how">How it works</a><a href="/#pricing">Pricing</a><a href="/#faq">FAQ</a></nav><form action="/diagnose" className="hidden min-w-0 max-w-64 flex-1 items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-slate-500 md:flex dark:border-slate-700 dark:bg-slate-900"><Search size={17}/><input name="q" aria-label="Search FixMind" placeholder="Search devices or issues" className="min-w-0 flex-1 bg-transparent text-sm text-slate-900 outline-none placeholder:text-slate-400 dark:text-white"/></form><div className="hidden items-center gap-2 md:flex">{authenticated?<form action={logout}><button className="btn btn-secondary !h-11 dark:border-slate-700 dark:bg-slate-900 dark:text-white"><LogOut size={16}/>Log out</button></form>:<Link href="/login" className="px-2 text-sm font-semibold text-slate-700 dark:text-slate-200">Log in</Link>}<Link href="/diagnose" className="btn btn-primary">Get started <ArrowRight size={16}/></Link></div><button aria-label="Toggle menu" className="grid h-11 w-11 place-items-center rounded-xl md:hidden dark:text-white" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="container grid gap-4 border-t py-5 md:hidden dark:border-slate-800 dark:bg-slate-950"><form action="/diagnose" className="flex items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 dark:border-slate-700 dark:bg-slate-900"><Search size={18}/><input name="q" aria-label="Search FixMind" placeholder="Search devices or issues" className="min-w-0 flex-1 bg-transparent text-sm outline-none dark:text-white"/></form><a href="/#features">Features</a><a href="/#how">How it works</a><a href="/#faq">FAQ</a><Link href="/diagnose" className="btn btn-primary">Diagnose now</Link>{authenticated&&<form action={logout}><button className="btn btn-secondary w-full dark:border-slate-700 dark:bg-slate-900 dark:text-white"><LogOut size={18}/>Log out</button></form>}</div>}</header>}
export function StatCard({icon,title,value,trend}:{icon:ReactNode,title:string,value:string,trend?:string}){return <div className="card"><div className="mb-5 flex items-start justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-blue-600">{icon}</span>{trend&&<span className="badge badge-green">{trend}</span>}</div><p className="m-0 text-sm text-slate-500">{title}</p><p className="tabular mt-2 mb-0 text-3xl font-bold tracking-tight">{value}</p></div>}
export function PageTitle({eyebrow,title,copy}:{eyebrow?:string,title:string,copy?:string}){return <div className="mb-8">{eyebrow&&<p className="eyebrow">{eyebrow}</p>}<h1 className="h1 my-2 font-bold">{title}</h1>{copy&&<p className="muted m-0 max-w-2xl text-lg">{copy}</p>}</div>}
export function Stepper({current}:{current:number}){return <div className="mx-auto flex max-w-2xl items-center justify-between">{["Device","Service","Schedule","Confirm"].map((s,i)=><div className="relative flex flex-1 items-center last:flex-none" key={s}><div className="z-10 text-center"><span className={`grid h-8 w-8 place-items-center rounded-full text-sm font-bold ${i+1<current?"bg-green-500 text-white":i+1===current?"bg-blue-600 text-white":"bg-slate-200 text-slate-500"}`} style={i+1===current?{animation:"pulseRing 1.8s infinite"}:{}}>{i+1<current?<Check size={16}/>:i+1}</span><span className="mt-2 hidden text-xs font-semibold sm:block">{s}</span></div>{i<3&&<span className={`-ml-1 mr-1 h-0.5 flex-1 ${i+1<current?"bg-green-500":"bg-slate-200"}`}/>}</div>)}</div>}
export function AIAvatar(){return <span className="grid h-11 w-11 place-items-center rounded-full bg-gradient-to-br from-blue-600 to-cyan-500 text-white"><Sparkles size={20}/></span>}
