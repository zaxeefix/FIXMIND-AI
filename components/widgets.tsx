"use client";import {useState} from "react";import {ChevronDown} from "lucide-react";
export function Accordion(){const qs=[["How accurate is the AI diagnosis?","FixMind compares symptoms against verified repair patterns and always displays a confidence score. A professional technician confirms the fault before any booked repair begins."],["Is the diagnosis free?","Yes. You can receive likely causes, safety guidance, cost ranges, and estimated repair time without paying or creating an account."],["Which devices are supported?","FixMind supports current and recent smartphones, tablets, and laptops from Apple, Samsung, Google, Dell, HP, Lenovo, and other leading manufacturers."],["How are repair costs estimated?","Estimates combine typical part prices, local labour ranges, device complexity, and the diagnosed fault. Your technician confirms the final quote before work starts."],["Can I book a technician after diagnosis?","Yes. Diagnosis details carry directly into booking, where you can select a service location, appointment time, and qualified technician without re-entering information."],["How does FixMind protect my information?","Device details are encrypted in transit, access is limited by role, and FixMind never asks for device passwords during AI diagnosis."]];const[o,setO]=useState(0);return <div className="mt-10 divide-y divide-slate-200 border-y border-slate-200 dark:divide-slate-800 dark:border-slate-800">{qs.map(([q,a],i)=><div key={q}><button onClick={()=>setO(o===i?-1:i)} className="flex w-full items-center justify-between gap-4 bg-transparent py-6 text-left font-semibold text-slate-950 dark:text-white" aria-expanded={o===i}>{q}<ChevronDown className={`shrink-0 transition ${o===i?"rotate-180":""}`}/></button>{o===i&&<p className="mt-0 pb-6 leading-7 text-slate-600 dark:text-slate-400">{a}</p>}</div>)}</div>}
